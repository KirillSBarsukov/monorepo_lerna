{
  "name": "monorepo_lerna",
  "private": true,
  "devDependencies": {
    "@types/node": "^14.0.1",
    "lerna": "^3.21.0",
    "typescript": "^3.9.2"
  },
  "scripts": {
    "release": "lerna run build && lerna publish"
  },
  "workspaces": [
    "packages/*"
  ]
}
